<div class="col-sm-10 col-sm-offset-1">

  <div class="group">
    <%= link_to("Edit", edit_group_path(@group), class: "btn btn-md btn-default pull-right") %>
  </div>

  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
